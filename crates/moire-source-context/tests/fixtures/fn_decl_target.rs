fn helper() {}

👉 fn spawn_worker(
    task_name: &'static str,
    first_name: &'static str,
    first: u32,
    second_name: &'static str,
    second: u32,
    ready: bool,
    done_tx: u32,
) {
    let x = 1;
}

fn other() {}
